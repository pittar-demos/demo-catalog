kind: Application
apiVersion: argoproj.io/v1beta1
metadata:
  annotations:
    # Waves 0-99:    Cluster Config
    # Waves 100-199: Tenant namespaces
    # Waves 200-299: Operator Installation
    # Waves 300-399: Operator Instances and Cluster Services
    argocd.argoproj.io/sync-wave: "303"
  labels:
    gitops.ownedBy: openshift-gitops
    category: operators
  name: rhacs-central-secured
spec:
  destination:
    namespace: stackrox
    server: https://kubernetes.default.svc
  project: default
  source:
    path: advanced-cluster-security-instances/overlays/default
    repoURL: https://github.com/pittar-demos/demo-catalog.git
    targetRevision: main
  syncPolicy:
    automated:
      prune: false
      selfHeal: true
    syncOptions:
      - ApplyOutOfSyncOnly=true
    retry:
      limit: 5 # number of failed sync attempt retries; unlimited number of attempts if less than 0
      backoff:
        duration: 5s # the amount to back off. Default unit is seconds, but could also be a duration (e.g. "2m", "1h")
        factor: 2 # a factor to multiply the base duration after each failed retry
        maxDuration: 10m # the maximum amount of time allowed for the backoff strategy
